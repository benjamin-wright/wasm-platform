# Build Tool

- `wp init` - bootstrap a new project
- `wp create function` - bootstrap a new function
- `wp create migration <name>` - template a new SQL migrations file
- `wp build` - `docker run` a dockerfile with the framework WIT embedded, volume mounting the function and bin output dir
