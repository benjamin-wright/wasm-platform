apiVersion: db-operator.benjamin-wright.github.com/v1alpha1
kind: RedisDatabase
metadata:
  name: {{ .Release.Name }}-redis
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wasm-platform.databases.labels" . | nindent 4 }}
spec:
  storageSize: {{ .Values.databases.redis.storageSize }}
---
apiVersion: db-operator.benjamin-wright.github.com/v1alpha1
kind: RedisCredential
metadata:
  name: {{ .Release.Name }}-redis-wp-operator
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wasm-platform.databases.labels" . | nindent 4 }}
spec:
  databaseRef: {{ .Release.Name }}-redis
  username: {{ .Values.databases.redis.wpOperator.username }}
  secretName: {{ .Values.databases.redis.wpOperator.secretName }}
  aclCategories:
    - all
---
apiVersion: db-operator.benjamin-wright.github.com/v1alpha1
kind: RedisCredential
metadata:
  name: {{ .Release.Name }}-redis-execution-host
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wasm-platform.databases.labels" . | nindent 4 }}
spec:
  databaseRef: {{ .Release.Name }}-redis
  username: {{ .Values.databases.redis.executionHost.username }}
  secretName: {{ .Values.databases.redis.executionHost.secretName }}
  keyPatterns:
    - "*"
  aclCategories:
    - all
