apiVersion: apps/v1
kind: Deployment
metadata:
  name: module-cache
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wasm-platform.module-cache.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.moduleCache.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: module-cache
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        {{- include "wasm-platform.module-cache.labels" . | nindent 8 }}
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: module-cache
          image: "{{ .Values.moduleCache.image.repository }}:{{ .Values.moduleCache.image.tag }}"
          imagePullPolicy: {{ .Values.moduleCache.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          env:
            - name: RUST_LOG
              value: {{ .Values.moduleCache.logLevel | quote }}
          resources:
            {{- toYaml .Values.moduleCache.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
